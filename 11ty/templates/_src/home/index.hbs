<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ home.pageTitle }}</title>
</head>
<body>
  <h1>{{ home.pageTitle }}</h1>
  <img src="{{ home.heroImage }}" alt="{{ home.pageTitle }} hero image">
</body>
</html>
